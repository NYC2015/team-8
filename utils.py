from donor.models import Store
import random

def generateCouponCode():
    code = None
    while code is None
        x = random.randint(0,16777215)
        coupon = Coupon(code=x)
        if coupon.objects:
            coupon = code
    return code
